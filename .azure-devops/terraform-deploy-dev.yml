trigger:
  tags:
    include:
      - v*

pr: none

pool:
  vmImage: "ubuntu-latest"

variables:
  - group: nachet-dev

stages:
  - stage: InitAndPlan
    displayName: "Initialize and Plan"
    jobs:
      - job: TerraformInitPlan
        displayName: "Terraform Init and Plan"
        steps:
          - bash: |
              echo "Public IP address of Azure DevOps agent:"
              AGENT_IP=$(curl -s ifconfig.me)
              echo $AGENT_IP
              echo "##vso[task.setvariable variable=agentIP]$AGENT_IP"
              echo ""
            displayName: "Display Agent Public IP"

          - task: AzureCLI@2
            displayName: "Azure CLI Login and add the current agent IP to SA FW"
            inputs:
              azureSubscription: $(serviceConnectionName)
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                echo "Successfully logged in with Azure CLI"
                echo "Adding Agent IP $(agentIP) to storage account firewall..."
                az storage account network-rule add \
                  --resource-group $(resourceGroupName) \
                  --account-name $(storageAccountName) \
                  --ip-address $(agentIP) \
                  --output none
                echo "Waiting for firewall rule to propagate (60 seconds for safety)..."
                sleep 120

          - task: DownloadSecureFile@1
            name: DownloadSecureVars
            displayName: "Download nachet-dev.tfvars"
            inputs:
              secureFile: "nachet-dev.tfvars"

          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
            displayName: "Install Terraform (latest)"
            inputs:
              terraformVersion: 'latest'

          - bash: |
              echo "Public IP address of Azure DevOps agent (2):"
              AGENT_IP_2=$(curl -s ifconfig.me)
              echo $AGENT_IP_2
              echo "##vso[task.setvariable variable=agentIP]$AGENT_IP_2"
              echo ""
            displayName: "Display Agent Public IP (2)"

          - task: TerraformTask@5
            displayName: "Terraform Init"
            inputs:
              provider: "azurerm"
              command: "init"
              workingDirectory: "$(System.DefaultWorkingDirectory)/terraform/dev"
              backendServiceArm: "$(serviceConnectionName)"
              backendAzureRmResourceGroupName: "$(resourceGroupName)"
              backendAzureRmStorageAccountName: "$(storageAccountName)"
              backendAzureRmContainerName: "$(containerName)"
              backendAzureRmKey: "$(stateKey)"

          - task: TerraformTask@5
            displayName: "Terraform Plan"
            inputs:
              provider: "azurerm"
              command: "plan"
              commandOptions: "-var-file=$(DownloadSecureVars.SecureFilePath)"
              workingDirectory: "$(System.DefaultWorkingDirectory)/terraform/dev"
              environmentServiceNameAzureRM: "$(serviceConnectionName)"

          - task: AzureCLI@2
            displayName: "Remove agent IP from SA FW"
            condition: always()
            inputs:
              azureSubscription: $(serviceConnectionName)
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                echo "Removing Agent IP $(agentIP) from storage account firewall..."
                az storage account network-rule remove \
                  --resource-group $(resourceGroupName) \
                  --account-name $(storageAccountName) \
                  --ip-address $(agentIP) \
                  --output none || echo "IP might already be removed or doesn't exist"
                echo "Cleanup completed"

  - stage: Apply
    displayName: "Apply Terraform Plan"
    dependsOn: InitAndPlan
    condition: succeeded()
    jobs:
      - deployment: TerraformApply
        displayName: "Terraform Apply"
        pool:
          vmImage: "ubuntu-latest"
        environment: "TFApproval"
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self

                - bash: |
                    echo "Public IP address of Azure DevOps agent:"
                    AGENT_IP=$(curl -s ifconfig.me)
                    echo $AGENT_IP
                    echo "##vso[task.setvariable variable=agentIP]$AGENT_IP"
                    echo ""
                  displayName: "Display Agent Public IP"

                - task: AzureCLI@2
                  displayName: "Azure CLI Login and add the current agent IP to SA FW"
                  inputs:
                    azureSubscription: $(serviceConnectionName)
                    scriptType: "bash"
                    scriptLocation: "inlineScript"
                    inlineScript: |
                      echo "Successfully logged in with Azure CLI"
                      echo "Adding Agent IP $(agentIP) to storage account firewall..."
                      az storage account network-rule add \
                        --resource-group $(resourceGroupName) \
                        --account-name $(storageAccountName) \
                        --ip-address $(agentIP) \
                        --output none
                      echo "Waiting for firewall rule to propagate..."
                      sleep 120

                - task: DownloadSecureFile@1
                  name: DownloadSecureVars
                  displayName: "Download nachet-dev.tfvars"
                  inputs:
                    secureFile: "nachet-dev.tfvars"

                - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
                  displayName: "Install Terraform (latest)"
                  inputs:
                    terraformVersion: 'latest'

                - task: TerraformTask@5
                  displayName: "Terraform Init"
                  inputs:
                    provider: "azurerm"
                    command: "init"
                    workingDirectory: "$(System.DefaultWorkingDirectory)/terraform/dev"
                    backendServiceArm: "$(serviceConnectionName)"
                    backendAzureRmResourceGroupName: "$(resourceGroupName)"
                    backendAzureRmStorageAccountName: "$(storageAccountName)"
                    backendAzureRmContainerName: "$(containerName)"
                    backendAzureRmKey: "$(stateKey)"

                - task: TerraformTask@5
                  displayName: "Terraform Apply"
                  inputs:
                    provider: "azurerm"
                    command: "apply"
                    commandOptions: "-var-file=$(DownloadSecureVars.SecureFilePath)"
                    workingDirectory: "$(System.DefaultWorkingDirectory)/terraform/dev"
                    environmentServiceNameAzureRM: "$(serviceConnectionName)"

                - task: AzureCLI@2
                  displayName: "Remove agent IP from SA FW"
                  condition: always()
                  inputs:
                    azureSubscription: $(serviceConnectionName)
                    scriptType: "bash"
                    scriptLocation: "inlineScript"
                    inlineScript: |
                      echo "Removing Agent IP $(agentIP) from storage account firewall..."
                      az storage account network-rule remove \
                        --resource-group $(resourceGroupName) \
                        --account-name $(storageAccountName) \
                        --ip-address $(agentIP) \
                        --output none || echo "IP might already be removed or doesn't exist"
                      echo "Cleanup completed"
