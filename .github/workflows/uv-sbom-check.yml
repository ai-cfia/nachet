---
name: uv venv sbom check

permissions:
  contents: read
  pull-requests: write

on:
  workflow_call:
    inputs:
      working-directory:
        required: false
        type: string
        default: '.'

defaults:
  run:
    working-directory: ${{ inputs.working-directory }}

jobs:
  check-sbom:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install uv
        uses: astral-sh/setup-uv@142240426d0e82f3e584603c3aa194c582f5aed3

      - name: Install dependencies and cyclone-x
        run: |
          uv sync --dev

      - name: Generate new SBOM
        run: |
          uv run cyclonedx-py environment --output-reproducible -v --sv 1.6 \
            --pyproject pyproject.toml -o sbom-new.json

      - name: Check if SBOM needs update
        id: sbom-check
        run: |
          if ! cmp -s sbom.json sbom-new.json; then
            echo "SBOM file is outdated and needs to be regenerated"
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "## SBOM Update Required" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "The SBOM file (datastore/sbom.json) is outdated and " \
              "needs to be regenerated." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Run the following command to update it:" \
              >> $GITHUB_STEP_SUMMARY
            echo '```bash' >> $GITHUB_STEP_SUMMARY
            echo 'cd datastore && uv run cyclonedx-py environment ' \
              '--output-reproducible -v --sv 1.6 --pyproject pyproject.toml' \
              ' -o sbom.json' >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "SBOM file is up to date"
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "## SBOM Status" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "✅ SBOM file is up to date" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Show diff if needed
        if: steps.sbom-check.outputs.needs_update == 'true'
        run: |
          echo "Differences between current and expected SBOM:"
          diff sbom.json sbom-new.json || true

      - name: Fail if SBOM needs update
        if: steps.sbom-check.outputs.needs_update == 'true'
        run: |
          echo "SBOM file needs to be regenerated. Please run:"
          echo "cd datastore && uv run cyclonedx-py environment " \
            "--output-reproducible -v --sv 1.6 --pyproject pyproject.toml " \
            "-o sbom.json"
          exit 1
