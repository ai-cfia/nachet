# Scripts Directory

This directory contains various scripts used for development and maintenance tasks related to the Nachet backend application.

These scripts perform admin functions that are not exposed via API routes. They are intended to be run from the command line and can be executed using Python.

These scripts may be considered for inclusion in the API if we implement admin only routes in the future.
