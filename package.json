{
  "scripts": {
    "playwright:install-deps": "npx playwright install-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "playwright": "^1.54.2"
  }
}
